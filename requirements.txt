fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
tenacity==9.0.0
groq==0.15.0
qdrant-client==1.12.2
pytest==8.3.4
fastembed==0.8.0
pyyaml==6.0.3
bcrypt==4.2.1
pyjwt==2.10.1
numpy==2.4.6
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364

# --- Phase 5 evals (SPEC_1.md Sec 11.1-style fallback -- see PLAN.md task 5.4) ---
# `pip install ragas` alone resolves to ragas==0.4.3 + langchain==1.3.x, an untested
# combo where ragas.llms.base imports langchain_community.chat_models.vertexai, a path
# that no longer exists in current langchain-community -- ModuleNotFoundError on the
# bare `import ragas`. Pinning to a self-consistent, contemporary version set (ragas
# 0.2.15 with the langchain 0.3.27 family it actually shipped alongside) resolves
# cleanly. Verified this does not break any existing app import.
ragas==0.2.15
langchain==0.3.27
langchain-community==0.3.27
langchain-core==0.3.75
langchain-groq==0.3.7